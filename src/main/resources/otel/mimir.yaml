# Minimal Mimir config
multitenancy_enabled: false

common:
  storage:
    backend: local
    local:
      directory: /tmp/mimir

blocks_storage:
  backend: local
  local:
    directory: /tmp/mimir/blocks
  tsdb:
    dir: /tmp/mimir/tsdb

compactor:
  data_dir: /tmp/mimir/compactor
